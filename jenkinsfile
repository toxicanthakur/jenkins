pipeline{
    agent any
    stages{
        stage('BUILD'{
            steps{
                sh 'printenv'
                sh'docker build-t ecr-demoimg .' 
            }
        })
    }
}